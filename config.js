module.exports = {
    url: "http://localhost:3000/"

    // É possível passar qualquer site como valor, caso necessário.

    // Mas a ideia é justamente rodar o localhost para ver em tempo
    // real a aplicação que estamos desenvolvendo.
}
